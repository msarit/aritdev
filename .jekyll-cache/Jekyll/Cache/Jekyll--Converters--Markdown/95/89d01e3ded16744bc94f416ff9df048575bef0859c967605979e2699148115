I"<div class="container">
  <div class="row">
    <div class="plain-page-padding">
      <h1 class="font-weight-bold title h4 text-uppercase mb-4">Categories</h1>
      {% for category in site.categories %} 
        <div class="font-weight-bold text-uppercase mt-4 salmon" id="{{ category[0] | downcase }}">
          <span>{{ category[0] }}</span>
        </div>
        
        {% assign pages_list = category[1] %}
        {% for post in pages_list %}
          {% if post.title != null %}
            {% if group == null or group == post.group %}
              {% include category-card.html %}
            {% endif %}
          {% endif %}
        {% endfor %}
        {% assign pages_list = nil %}
        {% assign group = nil %}
      {% endfor %}
    </div>
  </div>
</div>
:ET